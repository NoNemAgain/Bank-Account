package com.DUONG.BankAccount.old.unitTest.service;

import org.junit.jupiter.api.extension.ExtendWith;
import org.mockito.junit.jupiter.MockitoExtension;


@ExtendWith(MockitoExtension.class)
public class OperationServiceTest {
    /*
    @Mock
    private OperationRepository operationRepository;


    @InjectMocks
    private OperationService operationService;


    @Test
    void createOperation_positive_amount_should_create_deposit_operation() {
        //GIVEN
        CheckingAccount checkingAccount = new CheckingAccount();

        //WHEN
        operationService.createOperationAndArchive(checkingAccount, 200);
        //THEN
        assertEquals("Dépot", checkingAccount.getOperationsHistory().get(0).getType());
        assertEquals(1, checkingAccount.getOperationsHistory().size());
        assertEquals(200, checkingAccount.getOperationsHistory().get(0).getMontant());
    }

    @Test
    void createOperation_negative_amount_should_create_withdraw_operation() {
        //GIVEN
        CheckingAccount checkingAccount = new CheckingAccount();
        //WHEN
        operationService.createOperationAndArchive(checkingAccount, -200);
        //THEN
        assertEquals("Retrait", checkingAccount.getOperationsHistory().get(0).getType());
        assertEquals(1, checkingAccount.getOperationsHistory().size());
        assertEquals(-200, checkingAccount.getOperationsHistory().get(0).getMontant());
    }

     */
}
