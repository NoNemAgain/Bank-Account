package com.DUONG.BankAccount.adapter.in.controller;


import org.springframework.web.bind.annotation.*;

@RestController
@RequestMapping("/api/checkingAccounts")
public class CheckingAccountController {
    /*
    private final CheckingAccountService checkingAccountService;

    public CheckingAccountController(CheckingAccountService checkingAccountService) {
        this.checkingAccountService = checkingAccountService;
    }

    @PatchMapping("/{id}/deposit")
    public ResponseEntity<BankAccount> depositMoney(@PathVariable Long id, @RequestParam double amount) {
        return ResponseEntity.ok(checkingAccountService.depositMoney(id, amount));
    }

    @PatchMapping("/{id}/withdraw")
    public ResponseEntity<BankAccount> withdrawMoney(@PathVariable Long id, @RequestParam double amount) {
        return ResponseEntity.ok(checkingAccountService.withdrawMoney(id, amount));
    }
    */

}
